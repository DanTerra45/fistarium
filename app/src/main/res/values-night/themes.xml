<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Fistarium" parent="Theme.AppCompat.NoActionBar">
        <!-- Edge-to-edge is handled by enableEdgeToEdge() in MainActivity -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_fistarium</item>
        <item name="postSplashScreenTheme">@style/Theme.Fistarium</item>
    </style>
</resources>